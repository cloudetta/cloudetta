# --- Domini pubblici (opzionali) ---
# email per ACME/Let's Encrypt
CADDY_EMAIL=admin@email.com
ACME_EMAIL=${CADDY_EMAIL}

DJANGO_DOMAIN=
ODOO_DOMAIN=
REDMINE_DOMAIN=
NEXTCLOUD_DOMAIN=
N8N_DOMAIN=
WIKI_DOMAIN=
MAUTIC_DOMAIN=
MATTERMOST_DOMAIN=

# ====== Admin unificato (usato ovunque) ======
ADMIN_USER=admin
ADMIN_PASS=ChangeMe!123
ADMIN_EMAIL=admin@email.com

# ====== Django ======
DJANGO_SECRET_KEY=dev_change_me
DJANGO_DB_PASSWORD=dev_django_pw
DJANGO_DEBUG=True

# devono combaciare con i domini nel Caddyfile
DJANGO_ALLOWED_HOSTS=django,localhost,127.0.0.1,django.localhost,django.example.com
DJANGO_CSRF_TRUSTED_ORIGINS=http://django.localhost,http://django.example.com,https://django.localhost,https://django.example.com
DJANGO_USE_X_FORWARDED_HOST=True
DJANGO_SECURE_PROXY_SSL_HEADER=HTTP_X_FORWARDED_PROTO,https

# admin Django (mappati da admin unificato)
DJANGO_ADMIN_USER=${ADMIN_USER}
DJANGO_ADMIN_EMAIL=${ADMIN_EMAIL}
DJANGO_ADMIN_PASS=${ADMIN_PASS}


# ====== Odoo ======
ODOO_DB_PASSWORD=dev_odoo_db_pw
# setup automatico
ODOO_DB=cloudetta
ODOO_MASTER_PASSWORD=${ADMIN_PASS}
ODOO_DEMO=true
ODOO_LANG=it_IT

# ====== Redmine ======
REDMINE_DB_PASSWORD=dev_redmine_db_pw
REDMINE_ROOT_PW=dev_redmine_root_pw
# viene generato dal bootstrap se mancante
REDMINE_SECRET_KEY_BASE=

# ====== Nextcloud ======
NEXTCLOUD_DB_PASSWORD=dev_nextcloud_db_pw
NEXTCLOUD_ROOT_PW=dev_nextcloud_root_pw
# admin Nextcloud (unificato)
NEXTCLOUD_ADMIN_USER=${ADMIN_USER}
NEXTCLOUD_ADMIN_PASS=${ADMIN_PASS}

# domini attesi (locale + eventuale prod)
PUBLIC_DOMAIN=
TRUSTED_DOMAINS=localhost,127.0.0.1,nextcloud,nextcloud.localhost,${PUBLIC_DOMAIN}

# ====== n8n ======
# per compat: lo stack usa ADMIN_* ma lasciamo anche questa var
N8N_PASSWORD=${ADMIN_PASS}

# ====== Mail ======
MAIL_PROVIDER=sendgrid   # sendgrid | mailcow | smtp
MAIL_USER=admin@example.com
MAIL_PASS=change_me_mail_password

# Email Configuration yahoo
# MAIL_PROVIDER=smtp
# MAIL_USER=admin@email.com
# MAIL_PASS=hgdxkvlxwnnkujsr
# MAIL_HOST=smtp.mail.yahoo.com
# MAIL_PORT=587
# MAIL_ENCRYPTION=tls
# MAIL_FROM_NAME="Antonio Trento"
# MAIL_FROM_ADDRESS=admin@email.com

# ====== Caddy/DokuWiki (Basic Auth) ======
# hash generato dal bootstrap se vuoto
WIKI_BCRYPT_HASH=

# ====== Mautic ======
# DB Mautic (il bootstrap usa default sicuri se lasci vuoto, ma è meglio impostarli)
MAUTIC_DB_PASSWORD=dev_mautic_db_pw
MAUTIC_ROOT_PW=dev_mautic_root_pw
# Dominio pubblico (opzionale, per HTTPS via Caddy)
# Se impostato, il bootstrap aggiorna site_url e il vhost
# MAUTIC_DOMAIN=mautic.example.com


# ====== Mattermost ======
MATTERMOST_DB_PASSWORD=dev_mattermost_db_pw
# Per locale lasciamo http://chat.localhost (Caddy). In prod metti https://chat.tuodominio.tld
MATTERMOST_SITEURL=http://chat.localhost
# Opzionale, per blocchi prod nel Caddyfile


# Admin Mattermost creato dal bootstrap via mmctl (local mode)
MATTERMOST_ADMIN_USER=${ADMIN_USER}
MATTERMOST_ADMIN_EMAIL=${ADMIN_EMAIL}
MATTERMOST_ADMIN_PASS=${ADMIN_PASS}
# Team di default
MATTERMOST_TEAM_NAME=cloudetta
MATTERMOST_TEAM_DISPLAY=Cloudetta
